# Censys API Credentials
# Register at https://censys.io/register to obtain these credentials
CENSYS_API_ID=your_api_id_here
CENSYS_API_SECRET=your_api_secret_here

# Optional Configuration
# Uncomment and modify as needed

# API timeout in seconds (default: 300)
# CENSYS_TIMEOUT=300

# Cache directory for API results (default: .cache/censys)
# CENSYS_CACHE_DIR=.cache/censys

# Default output directory (default: results)
# OUTPUT_DIR=results

# Default number of results per page (max 100, default: 50)
# DEFAULT_PAGE_SIZE=50

# Maximum number of pages to process (-1 for all pages, default: -1)
# MAX_PAGES=-1

# Default data freshness filter (options: 1, 3, 7, all; default: all)
# DATA_AGE=all

# Default output format (options: json, text; default: json)
# OUTPUT_FORMAT=json

# Debug mode (true/false, default: false)
# DEBUG=false

# Log file location (default: none, logs to console only)
# LOG_FILE=censyspy.log